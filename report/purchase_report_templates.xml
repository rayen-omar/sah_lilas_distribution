<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="report_purchaseorder_document_inherit_sah" inherit_id="purchase.report_purchaseorder_document">
        <!-- En-tête du tableau -->
        <xpath expr="//th[@name='th_quantity']" position="before">
            <th name="th_sah_qty_unit" class="text-end"><strong>Qté Unités</strong></th>
            <th name="th_sah_price_unit" class="text-end"><strong>PU Unité</strong></th>
        </xpath>

        <!-- Lignes du tableau -->
        <!-- Odoo 19 purchase report n'a pas de name sur td_quantity, on utilise le span enfant -->
        <xpath expr="//span[@t-field='line.product_qty']/.." position="before">
            <td name="td_sah_qty_unit" class="text-end">
                <span t-field="line.sah_qty_unit"/>
            </td>
            <td name="td_sah_price_unit" class="text-end">
                <span t-field="line.sah_price_unit" t-options='{"widget": "monetary", "display_currency": o.currency_id}'/>
            </td>
        </xpath>
        
        <!-- On ajuste le colspan pour les lignes section/note (il est à 99, donc pas de changement strict, mais par sécurité) -->
    </template>
</odoo>
